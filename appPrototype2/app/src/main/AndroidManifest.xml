<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appprototype1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".hive.HiveDetailsActivity"></activity>
        <activity android:name=".hive.NewHiveActivity" />
        <activity android:name=".hive.HivesActivity" />
        <activity android:name=".hive.NoHiveActivity" />
        <activity android:name=".password.NewPasswordActivity" />
        <activity android:name=".apiary.ApiariesActivity" />
        <activity android:name=".apiary.NewApiaryActivity" />
        <activity android:name=".apiary.NoApiariesActivity" />
        <activity android:name=".password.ForgotPassword1" />
        <activity android:name=".MainMenu" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Two">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>